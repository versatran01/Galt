set(Boost_INCLUDE_DIR /usr/include/boost)
set(Boost_LIBRARY_DIR /usr/lib/x86_64-linux-gnu/boost)
message(STATUS "BOOST_DIR" ${Boost_INCLUDE_DIR})
message(STATUS "BOOST_LIB" ${Boost_LIBRARY_DIR})
link_directories(${Boost_LIBRARY_DIR})
include_directories( ${Boost_INCLUDE_DIR} )

rosbuild_add_library(ublox_gps gps.cpp)
#rosbuild_link_boost(ublox_gps system)
target_link_libraries(ublox_gps boost_system)

rosbuild_add_executable(ublox_gps_node node.cpp)
set_target_properties(ublox_gps_node PROPERTIES OUTPUT_NAME ublox_gps)
#rosbuild_link_boost(ublox_gps_node regex)
target_link_libraries(ublox_gps_node boost_regex)
target_link_libraries(ublox_gps_node ublox_gps)
