#!/usr/bin/env python
PACKAGE = "flir_gige"

from dynamic_reconfigure.parameter_generator_catkin import *

gen = ParameterGenerator()

# name / type / level / description / default / min / max
# Flir A35 has a maximum resolution of 320 x 256
gen.add("celcius_min", double_t, 0, "min temperature", 20, 10, 35)
gen.add("celcius_max", double_t, 0, "max temperature", 50, 35, 60)

exit(gen.generate(PACKAGE, "flir_gige", "ProcDyn"))
